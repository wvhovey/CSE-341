// Create variable to display favorite name
const favoriteName1 = (req, res) => {

    // Create name
    res.send('Esmeralda Alfaro');
  };

  // Send variable to the router files
  module.exports = { 
    favoriteName1
};